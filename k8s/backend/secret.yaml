# Simple defaults: postgres/postgres. Apply with: kubectl apply -f secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: balderdash
type: Opaque
stringData:
  DATABASE_PASSWORD: "postgres"
  REDIS_PASSWORD: ""
  SECRET_KEY_BASE: "changeme-generate-with-mix-phx-gen-secret"
